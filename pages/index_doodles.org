#+title: doodles
#+title_extra: {{{index-navbar(Doodles)}}}

#+BEGIN_SRC elisp :results raw :exports results
(->> (ns/blog-get-metas-public)
     (--filter (ht-get it :is-doodle))
     (-map (-lambda ((&hash :slug :title))
	       (format "** [[./%s.org][%s]]" slug title)))
     (s-join "\n"))
#+END_SRC
